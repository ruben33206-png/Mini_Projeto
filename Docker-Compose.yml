services:
  api:
    image: "miniprojeto"
    container_name: "Mini_Projeto"
    expose:
      - 8000
    ports:
      - 8000:8000
    volumes:
      - ../API_Mini_Project:/API_Mini_Project
    command: tail -f /dev/null